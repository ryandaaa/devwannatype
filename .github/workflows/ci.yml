name: ci

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello from ci, sha=${{ github.sha }}, ref=${{ github.ref }}"
      - run: ls -la
